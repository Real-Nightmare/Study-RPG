{
  "name": "study-rpg-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Study RPG Backend - NestJS API for OpsCtrl deployment",
  "scripts": {
    "install:backend": "cd backend && npm install",
    "migrate": "cd backend && npm run migrate",
    "seed:admin": "cd backend && npm run seed:admin"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
