# =============================================================================
# Study RPG — OpsCtrl Configuration
# =============================================================================
# This branch is structured for OpsCtrl deployment.
# NestJS app is at repo root, migrations at ./migrations/
#
# Intended stack:
#   Frontend  → Cloudflare Pages (from main branch)
#   Backend   → OpsCtrl (auto-detects NestJS, auto-provisions PostgreSQL + Redis)
#   Database  → OpsCtrl PostgreSQL + pgvector
#   Cache     → OpsCtrl Redis
#   Storage   → Dark Storage / Synclyz / Backblaze B2 / Blomp / local
# =============================================================================
